# Frontend environment — copy to frontend/.env and adjust if needed.

# Base URL of the backend API the frontend calls (used by src/api/*).
# Local dev / Docker Compose default:
VITE_API_BASE_URL=http://localhost:8000
